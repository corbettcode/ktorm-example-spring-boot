
logging:
  level:
    root: INFO
    org:
      hibernate: DEBUG
      jdbc:
        datasource.init: DEBUG
      ktorm: DEBUG
      springframework.boot: INFO

spring:
  datasource:
    url: jdbc:h2:mem:test
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console.enabled: true
  jpa:
    hibernate.ddl-auto: none
  sql:
    init.mode: embedded
org:
  h2.Driver:
